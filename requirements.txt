# Dependencias de producao.
#
# OpenCV NAO entra aqui: a leitura do QR Code passou para o navegador
# (static/vendor/jsQR.js). No servidor ele custaria mais de 100 MB e
# estouraria o limite de tamanho da funcao serverless da Vercel.
# Para usar a linha de comando localmente: pip install -r requirements-dev.txt
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
flask>=3.0
gunicorn>=21.2
psycopg[binary]>=3.1
